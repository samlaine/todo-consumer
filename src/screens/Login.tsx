import * as React from 'react'
import { SafeAreaView, Text } from 'react-native'

export const Login: React.FC<any> = () => {
    return (
        <SafeAreaView>
            <Text>Login</Text>
        </SafeAreaView>
    )
}
