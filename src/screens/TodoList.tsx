import * as React from 'react'
import { SafeAreaView, Text } from 'react-native'

export const TodoList = () => {
    return (
        <SafeAreaView>
            <Text>TodoList</Text>
        </SafeAreaView>
    )
}
